# --- Profiles for Environment Management ---
spring:
  application:
    name: Emergency and Mental Wellbeing Authentication  # Application name
  profiles:
    active: ${ENV:production}  # Active profile for environments (e.g., development, production) convert this to properties
